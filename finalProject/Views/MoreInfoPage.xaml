<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:behaviors="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    prism:ViewModelLocator.AutowireViewModel="True"
    x:Class="finalProject.Views.MoreInfoPage">
	<StackLayout Padding="0,20,0,0" HorizontalOptions="Center">
        <Image Source="logo.png" />
        <Label Text="PetFinder" Font="Bold" FontSize="25" HorizontalTextAlignment="Center" />
        <Label Text="---------------------------------------------" />
        <Label Text="Find Pet by Adpotion Center" HorizontalOptions="Center"/>
         <Entry
                Placeholder="Enter Zip Code"
                Text="{Binding LocationEnteredByUser}"
                WidthRequest="200" />
        <Button
                Text="Search"
                Command="{Binding GetAdoptionForLocationCommand}" />
        <Label Text="See the Adoption Center on Maps" HorizontalOptions="Center"/>
        <Button Text="Map Me!" />
        <Button Text="Back"
            Command="{Binding GoBackCommand}" HorizontalOptions="Center"/>
        </StackLayout>
</ContentPage>
